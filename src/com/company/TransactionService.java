package com.company;

/*
public class TransactionService {
    public void processTransaction(int transactionId) {
        try {
            validateTransaction(transactionId);
        } catch (InvalidTransactionException e) {
            logger.log(Level.WARNING, "Transaction validation failed for ID: " + transactionId, e);
        }
    }

    private void validateTransaction(int transactionId) throws InvalidTransactionException {
        // Validation logic
        if (transactionId < 0) {
            throw new InvalidTransactionException("Transaction ID cannot be negative");
        }
    }
}
 */